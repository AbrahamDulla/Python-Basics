# Control flow refers to the order in which statements are executed in a program

# if statement:
# The if statement is used to make a decision based on a condition.
# If the condition is True, the statements inside the if block are executed.

# example

if (True):
    print("This should execute")

if (1+2 == 3):
    print("This also should execute since 1 + 2 = 3")

if (1+2 == 4):
    print("This shouldn't execute since 1 + 2 != 4")
